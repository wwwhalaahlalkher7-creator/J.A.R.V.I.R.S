# Current cloud target

**Primary test target: blitz.cloud**

Render is retained only as a fallback configuration. The current JARVIS cloud
Dockerfile is optimized for blitz.cloud's unprivileged runtime and port 8080.
